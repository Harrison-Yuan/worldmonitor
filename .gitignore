# 依赖与构建产物
node_modules/

# Cloudflare
.wrangler/
.dev.vars

# 本地环境
.env
.env.*
!.env.example

# 生成物(由 scripts/build-data-inline.mjs 生成,已提交以便 Workers 直接部署)
# 若不想提交,可取消注释并在 workflow/deploy 前先构建:
# lib/data-inline.mjs

# 日志
*.log
